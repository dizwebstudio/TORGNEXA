# Connector Conformance

Run the versioned conformance suite against manifest/capabilities/auth/health/errors/rate limits/pagination/idempotency/webhooks/reconciliation. Produce a report and block verified status on required failures.

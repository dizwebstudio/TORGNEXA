# Implement TORGNEXA Module

Use for internal domain/platform modules. Read AGENTS/module docs/ADR/task; identify invariants; update public contracts first; implement smallest change; test success/failure; run repository checks; update docs. Never leak tenant boundaries or provider-specific logic into core.

# Add/Change Kafka Event

Confirm event need; select domain topic/partition key; `.vN` name; add schema/envelope; document replay/PII; produce via outbox; consumers idempotent; compatibility tests; breaking change => new version.

# SIEM Export Change

Persist authoritative audit first, then export minimized SecurityEvent records asynchronously. Never block business commits on SIEM. Add retry/DLQ, redaction, sink health/lag and idempotent replay tests.

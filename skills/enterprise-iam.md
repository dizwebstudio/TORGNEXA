# Enterprise IAM Change

Keep Keycloak/OIDC as application boundary. External LDAP/AD/SAML/SCIM/JIT identity data maps through explicit reviewed role/tenant rules with default deny. Test offboarding, group drift, service-account scope and privileged audit.

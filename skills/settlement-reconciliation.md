# Settlement Reconciliation

Normalize provider financial facts into append-oriented ledger entries, import deterministic fixtures, match to orders/returns/payouts, classify differences and never silently mutate historical entries.

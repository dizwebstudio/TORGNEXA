# Privacy / Data Governance Change

Classify touched data, document purpose/retention, minimize PII in events/logs, implement deletion/retention propagation, add tenant isolation tests and update privacy contracts. Never use production PII fixtures.

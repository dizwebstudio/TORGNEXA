# Implement Marketplace Connector

Create current Connector Spec from official docs; declare capabilities; use secret/HTTP abstractions; implement reads before writes; handle remote IDs, timeouts, quotas, normalized errors, idempotency/dry-run and reconciliation; add deterministic fixtures. Never branch Core on provider.

# Developer Platform Change

Update contract-first OpenAPI/webhooks/SDK docs, examples and compatibility policy. Public SDKs must use public API models, scoped credentials and deterministic integration fixtures.

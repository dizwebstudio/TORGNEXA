# Implement Government/Compliance Connector

Use current official docs only; classify risk; keep remote authoritative status distinct; signatures only through Signing Service; full audit/correlation; default sensitive writes to approval; add reconciliation/error-code tests.

# Release Security

Run tests plus secret/dependency/license/container checks; produce SBOM, signed artifacts/provenance and compatibility notes. Treat critical unresolved findings as release blockers unless explicitly risk-accepted.

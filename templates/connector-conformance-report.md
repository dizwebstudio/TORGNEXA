# Connector Conformance Release Note

The canonical machine-readable evidence is `docs/connectors/<connector-id>/conformance-report.json` using `contracts/plugins/conformance-report-v1.schema.json`. Do not reproduce secrets or raw provider failures in this human note.

## Connector / version / SDK

## Machine report path and SHA-256

## Capability / manifest review

## Required Task-064 checks

All thirteen machine checks must be `pass`; required checks cannot be waived or skipped.

## Provider-specific semantic tests

## Known limitations outside Task-064 scope

## Approval / trust level

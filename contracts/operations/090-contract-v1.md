# Task 090 contract v1

CDEK proves the Logistics SDK with rates, shipment create/cancel/track, labels, pickup points and returns while keeping remote tariff/service ids inside the adapter.

All tenant data is organization/workspace scoped, retry/idempotency semantics are explicit, and credentials/PII are minimized behind existing security boundaries.

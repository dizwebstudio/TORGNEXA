# 075 PUDO contract v1
- registry: own/external point, active state, capacity;
- lifecycle: created/arrived/ready/issued or expired/return_pending/returned;
- invalid transitions fail closed;
- issue/return expose payment/fiscal/report hooks;
- event evidence is append-only and tenant-scoped.

{
  "id": "sbp",
  "name": "SBP",
  "family": "payment",
  "version": "1.0.0",
  "sdk_version": 1,
  "capabilities": [
    "payments.create",
    "payments.refund",
    "payments.reconcile",
    "payments.status.read",
    "payments.webhooks"
  ],
  "auth": [
    {
      "kind": "certificate",
      "secret_class": "payment.certificate",
      "required": true
    }
  ],
  "rate_limit": {
    "max_concurrency": 2,
    "min_interval_ms": 200,
    "request_timeout_ms": 30000,
    "retry": {
      "max_attempts": 4,
      "base_backoff_ms": 500,
      "max_backoff_ms": 30000
    }
  }
}

{
  "id": "rb-auth",
  "title": "Auth incident",
  "trigger_codes": [
    "auth_failure_spike"
  ],
  "steps": [
    {
      "id": "contain",
      "safe_action": "Disable the affected federation mapping or rotate a compromised client credential through approved secret workflow.",
      "validation": "Verify OIDC login, denied unmapped identities and revoked sessions.",
      "rollback": "Restore the last reviewed mapping or credential reference.",
      "evidence": "security audit ids and authentication probes"
    }
  ]
}

# Saleor

Storefront connector. Self-hosted, GraphQL-only API, a single long-lived App access token, products/inventory/prices/orders/returns. Product create and webhook receipt are unsupported by design (see capability-audit.md); product/price/inventory writes require an admin-configured channel and warehouse slug.

Official documentation: https://docs.saleor.io/api-reference/

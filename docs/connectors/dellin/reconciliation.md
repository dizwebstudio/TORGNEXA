# Reconciliation

До квалификации операций runtime не создаёт заявки и не опрашивает заказы.
После admission статусы «Деловых Линий» должны считаться удалённым
источником истины, а неоднозначные ответы — попадать в reconciliation, а не
повторяться вслепую.

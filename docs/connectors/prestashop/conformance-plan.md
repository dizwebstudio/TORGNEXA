# PrestaShop conformance plan

Run the standard Task-064 13-check suite against `prestashop.NewConformanceCandidate` with the Task-029 emulator. Provider-specific deterministic tests additionally validate combination projection and StockAvailable PATCH behavior.

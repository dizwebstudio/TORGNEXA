// Code generated by torgnexa-sdkgen/v1; DO NOT EDIT.
// Source: contracts/openapi/torgnexa-v1.yaml sha256=59d8507f4f0dd9e27adcccfc4b6f9f6f0c20298f8873b5924a0023e702b0e899

export const API_VERSION = "0.21.2";
export const OPENAPI_VERSION = "3.1.0";
export const DEFAULT_SERVER_PATH = "/api/v1";
export const GENERATOR_VERSION = "torgnexa-sdkgen/v1";

export class APIError extends Error {
  constructor(statusCode, body) {
    super(`TORGNEXA API returned HTTP ${statusCode}`);
    this.name = "APIError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export class TorgnexaClient {
  constructor(config) {
    if (!config || typeof config.baseURL !== "string" || config.baseURL.trim() === "") throw new TypeError("baseURL is required");
    const parsed = new URL(config.baseURL);
    if (parsed.protocol !== "http:" && parsed.protocol !== "https:") throw new TypeError("baseURL must use http or https");
    parsed.search = "";
    parsed.hash = "";
    this.baseURL = parsed.toString().replace(/\/$/, "");
    this.bearerToken = config.bearerToken || "";
    this.fetchImpl = config.fetch || globalThis.fetch;
    if (typeof this.fetchImpl !== "function") throw new TypeError("fetch implementation is required");
    this.userAgent = config.userAgent || ("torgnexa-typescript-sdk/" + API_VERSION);
  }

  async _request(method, path, query, body, options = {}) {
    const endpoint = new URL(this.baseURL + path);
    for (const [key, value] of Object.entries(query || {})) {
      if (value !== undefined && value !== null && value !== "" && value !== 0 && value !== false) endpoint.searchParams.set(key, String(value));
    }
    const headers = { Accept: "application/json", ...(options.headers || {}) };
    if (body !== undefined && body !== null) headers["Content-Type"] = "application/json";
    if (this.bearerToken) headers.Authorization = "Bearer " + this.bearerToken;
    const raw = await this.fetchImpl(endpoint, { method, headers, body: body === undefined || body === null ? undefined : JSON.stringify(body), signal: options.signal });
    let payload = null;
    const contentType = (raw.headers.get("content-type") || "").split(";", 1)[0].trim().toLowerCase();
    const binary = contentType !== "" && !contentType.startsWith("text/") && !contentType.includes("json") && !contentType.includes("xml");
    if (binary) {
      payload = await raw.arrayBuffer();
    } else {
      const text = await raw.text();
      if (text !== "") { try { payload = JSON.parse(text); } catch { payload = text; } }
    }
    const response = { statusCode: raw.status, headers: raw.headers, body: payload };
    if (raw.status < 200 || raw.status >= 300) throw new APIError(raw.status, payload);
    return response;
  }

  async activateSettingsIdentityProvider(input = {}, options = {}) {
    let path = "/settings/identity-providers/{provider_id}:activate";
    if (typeof input.providerId !== "string" || input.providerId.trim() === "") throw new TypeError("provider_id is required");
    path = path.replace("{provider_id}", encodeURIComponent(input.providerId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async adjustInventoryPosition(input = {}, options = {}) {
    let path = "/inventory/positions/{position_id}/adjustments";
    if (typeof input.positionId !== "string" || input.positionId.trim() === "") throw new TypeError("position_id is required");
    path = path.replace("{position_id}", encodeURIComponent(input.positionId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async advanceWarehouseMobilePlan(input = {}, options = {}) {
    let path = "/warehouse-mobile/plans/{plan_id}/advance";
    if (typeof input.planId !== "string" || input.planId.trim() === "") throw new TypeError("plan_id is required");
    path = path.replace("{plan_id}", encodeURIComponent(input.planId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async analyzeWithAIProvider(input = {}, options = {}) {
    let path = "/settings/ai-providers:analyze";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async appendFinancialBankAccount(input = {}, options = {}) {
    let path = "/financial-completeness/accounts";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async appendFinancialBankStatement(input = {}, options = {}) {
    let path = "/financial-completeness/statements";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async appendFinancialCOGSBackfill(input = {}, options = {}) {
    let path = "/financial-completeness/cogs-backfills";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async appendFinancialSource(input = {}, options = {}) {
    let path = "/financial-completeness/sources";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async applyCatalogBulk(input = {}, options = {}) {
    let path = "/catalog/bulk/apply";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async applyMarketplaceGrowthOperation(input = {}, options = {}) {
    let path = "/marketplace-growth/operations";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async applyMarketplaceListingBatch(input = {}, options = {}) {
    let path = "/marketplace-listings/batch/apply";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async applyMarketplaceOperationFindingAction(input = {}, options = {}) {
    let path = "/marketplace-operations/findings/{finding_id}/actions";
    if (typeof input.findingId !== "string" || input.findingId.trim() === "") throw new TypeError("finding_id is required");
    path = path.replace("{finding_id}", encodeURIComponent(input.findingId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async applyMarketplaceOperationFlowCommand(input = {}, options = {}) {
    let path = "/marketplace-operations/flows/{flow_id}/commands";
    if (typeof input.flowId !== "string" || input.flowId.trim() === "") throw new TypeError("flow_id is required");
    path = path.replace("{flow_id}", encodeURIComponent(input.flowId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async approveAssistantActionPreview(input = {}, options = {}) {
    let path = "/assistant/action-previews/{preview_id}:approve";
    if (typeof input.previewId !== "string" || input.previewId.trim() === "") throw new TypeError("preview_id is required");
    path = path.replace("{preview_id}", encodeURIComponent(input.previewId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async approveProcurementPurchaseOrder(input = {}, options = {}) {
    let path = "/procurement/purchase-orders/{purchase_order_id}/approve";
    if (typeof input.purchaseOrderId !== "string" || input.purchaseOrderId.trim() === "") throw new TypeError("purchase_order_id is required");
    path = path.replace("{purchase_order_id}", encodeURIComponent(input.purchaseOrderId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async archiveLogisticsBatch(input = {}, options = {}) {
    let path = "/logistics/batches/archive/{batch_id}";
    if (typeof input.batchId !== "string" || input.batchId.trim() === "") throw new TypeError("batch_id is required");
    path = path.replace("{batch_id}", encodeURIComponent(input.batchId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async archiveWorkflow(input = {}, options = {}) {
    let path = "/workflow-commands/archive";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async assignCustomerServiceConversation(input = {}, options = {}) {
    let path = "/customer-service/assignments";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async assignProductCategory(input = {}, options = {}) {
    let path = "/products/{product_id}/categories/{category_id}";
    if (typeof input.productId !== "string" || input.productId.trim() === "") throw new TypeError("product_id is required");
    path = path.replace("{product_id}", encodeURIComponent(input.productId));
    if (typeof input.categoryId !== "string" || input.categoryId.trim() === "") throw new TypeError("category_id is required");
    path = path.replace("{category_id}", encodeURIComponent(input.categoryId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async calculateLogisticsRates(input = {}, options = {}) {
    let path = "/logistics/rates";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async cancelAssistantRun(input = {}, options = {}) {
    let path = "/assistant/runs/{run_id}:cancel";
    if (typeof input.runId !== "string" || input.runId.trim() === "") throw new TypeError("run_id is required");
    path = path.replace("{run_id}", encodeURIComponent(input.runId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async cancelLogisticsBatch(input = {}, options = {}) {
    let path = "/logistics/batches/cancel/{batch_id}";
    if (typeof input.batchId !== "string" || input.batchId.trim() === "") throw new TypeError("batch_id is required");
    path = path.replace("{batch_id}", encodeURIComponent(input.batchId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async cancelLogisticsShipment(input = {}, options = {}) {
    let path = "/logistics/shipments/{shipment_id}/cancel";
    if (typeof input.shipmentId !== "string" || input.shipmentId.trim() === "") throw new TypeError("shipment_id is required");
    path = path.replace("{shipment_id}", encodeURIComponent(input.shipmentId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async cancelProcurementPurchaseOrder(input = {}, options = {}) {
    let path = "/procurement/purchase-orders/{purchase_order_id}/cancel";
    if (typeof input.purchaseOrderId !== "string" || input.purchaseOrderId.trim() === "") throw new TypeError("purchase_order_id is required");
    path = path.replace("{purchase_order_id}", encodeURIComponent(input.purchaseOrderId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async cancelWarehouseTask(input = {}, options = {}) {
    let path = "/warehouse-tasks/{task_id}/cancel";
    if (typeof input.taskId !== "string" || input.taskId.trim() === "") throw new TypeError("task_id is required");
    path = path.replace("{task_id}", encodeURIComponent(input.taskId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async cancelWorkflowRun(input = {}, options = {}) {
    let path = "/workflow-run-commands/cancel";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async changeOrderCancellationStatus(input = {}, options = {}) {
    let path = "/order-cancellations/{cancellation_id}/status";
    if (typeof input.cancellationId !== "string" || input.cancellationId.trim() === "") throw new TypeError("cancellation_id is required");
    path = path.replace("{cancellation_id}", encodeURIComponent(input.cancellationId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async changeOrderStatus(input = {}, options = {}) {
    let path = "/orders/{order_id}/status";
    if (typeof input.orderId !== "string" || input.orderId.trim() === "") throw new TypeError("order_id is required");
    path = path.replace("{order_id}", encodeURIComponent(input.orderId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async changeReturnStatus(input = {}, options = {}) {
    let path = "/returns/{return_id}/status";
    if (typeof input.returnId !== "string" || input.returnId.trim() === "") throw new TypeError("return_id is required");
    path = path.replace("{return_id}", encodeURIComponent(input.returnId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async checkConnectorAccount(input = {}, options = {}) {
    let path = "/connector-accounts:check";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async claimWarehouseTask(input = {}, options = {}) {
    let path = "/warehouse-tasks/{task_id}/claim";
    if (typeof input.taskId !== "string" || input.taskId.trim() === "") throw new TypeError("task_id is required");
    path = path.replace("{task_id}", encodeURIComponent(input.taskId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async closeWarehouseMobilePack(input = {}, options = {}) {
    let path = "/warehouse-mobile/packs/{pack_id}/close";
    if (typeof input.packId !== "string" || input.packId.trim() === "") throw new TypeError("pack_id is required");
    path = path.replace("{pack_id}", encodeURIComponent(input.packId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async commitProcurementPriceList(input = {}, options = {}) {
    let path = "/procurement/price-lists/commit";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async completeConnectorOAuth(input = {}, options = {}) {
    let path = "/connector-accounts:oauth-callback";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async completeWarehouseTask(input = {}, options = {}) {
    let path = "/warehouse-tasks/{task_id}/complete";
    if (typeof input.taskId !== "string" || input.taskId.trim() === "") throw new TypeError("task_id is required");
    path = path.replace("{task_id}", encodeURIComponent(input.taskId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createAIProviderAccount(input = {}, options = {}) {
    let path = "/settings/ai-providers";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createApprovalPolicy(input = {}, options = {}) {
    let path = "/approval-policies";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createApprovalRequest(input = {}, options = {}) {
    let path = "/approvals/requests";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createAssistantSession(input = {}, options = {}) {
    let path = "/assistant/sessions";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createCatalogCategory(input = {}, options = {}) {
    let path = "/catalog/categories";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createConnectorAccount(input = {}, options = {}) {
    let path = "/connector-accounts";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createCounterparty(input = {}, options = {}) {
    let path = "/counterparties";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createCurrentUserProfilePrivacyRequest(input = {}, options = {}) {
    let path = "/me/profile/privacy-requests";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createDemoApproval(input = {}, options = {}) {
    let path = "/approvals/demo";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async createDemoOrders(input = {}, options = {}) {
    let path = "/orders/demo";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async createEcosystemOnboarding(input = {}, options = {}) {
    let path = "/ecosystem/onboarding";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createEcosystemPartnerCertification(input = {}, options = {}) {
    let path = "/ecosystem/partners/certifications";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createInventoryWarehouse(input = {}, options = {}) {
    let path = "/inventory/warehouses";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createLegalParty(input = {}, options = {}) {
    let path = "/legal-parties";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createLogisticsBatch(input = {}, options = {}) {
    let path = "/logistics/batches";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createLogisticsSeparateReturn(input = {}, options = {}) {
    let path = "/logistics/returns/separate";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createLogisticsShipment(input = {}, options = {}) {
    let path = "/logistics/shipments";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createMCPAccount(input = {}, options = {}) {
    let path = "/settings/mcp-accounts";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createMarketplaceGrowthPreview(input = {}, options = {}) {
    let path = "/marketplace-growth/previews";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createMarketplaceOperationFlow(input = {}, options = {}) {
    let path = "/marketplace-operations/flows";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createOfferPrice(input = {}, options = {}) {
    let path = "/products/{product_id}/prices/{offer_or_price_id}";
    if (typeof input.productId !== "string" || input.productId.trim() === "") throw new TypeError("product_id is required");
    path = path.replace("{product_id}", encodeURIComponent(input.productId));
    if (typeof input.offerOrPriceId !== "string" || input.offerOrPriceId.trim() === "") throw new TypeError("offer_or_price_id is required");
    path = path.replace("{offer_or_price_id}", encodeURIComponent(input.offerOrPriceId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createOrderCancellation(input = {}, options = {}) {
    let path = "/order-cancellations";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createPayment(input = {}, options = {}) {
    let path = "/payments";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createPrivacyRequest(input = {}, options = {}) {
    let path = "/privacy/requests";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createProcurementOffer(input = {}, options = {}) {
    let path = "/procurement/offers";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createProcurementPurchaseOrder(input = {}, options = {}) {
    let path = "/procurement/purchase-orders";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createProcurementPurchaseOrderFromRecommendations(input = {}, options = {}) {
    let path = "/procurement/purchase-orders/from-recommendations";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createProcurementSupplier(input = {}, options = {}) {
    let path = "/procurement/suppliers";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createProduct(input = {}, options = {}) {
    let path = "/products";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createProductImage(input = {}, options = {}) {
    let path = "/products/{product_id}/images";
    if (typeof input.productId !== "string" || input.productId.trim() === "") throw new TypeError("product_id is required");
    path = path.replace("{product_id}", encodeURIComponent(input.productId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createProductOffer(input = {}, options = {}) {
    let path = "/products/{product_id}/offers";
    if (typeof input.productId !== "string" || input.productId.trim() === "") throw new TypeError("product_id is required");
    path = path.replace("{product_id}", encodeURIComponent(input.productId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createProfitabilityScenario(input = {}, options = {}) {
    let path = "/profitability/scenarios";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createReconciliationJob(input = {}, options = {}) {
    let path = "/reconciliation/jobs";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createRefundAllocation(input = {}, options = {}) {
    let path = "/refund-allocations";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createReplenishmentRun(input = {}, options = {}) {
    let path = "/replenishment";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createReturn(input = {}, options = {}) {
    let path = "/returns";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createReturnItem(input = {}, options = {}) {
    let path = "/returns/{return_id}/items";
    if (typeof input.returnId !== "string" || input.returnId.trim() === "") throw new TypeError("return_id is required");
    path = path.replace("{return_id}", encodeURIComponent(input.returnId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createReturnLogistics(input = {}, options = {}) {
    let path = "/returns/{return_id}/logistics";
    if (typeof input.returnId !== "string" || input.returnId.trim() === "") throw new TypeError("return_id is required");
    path = path.replace("{return_id}", encodeURIComponent(input.returnId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createSellerFinancialCalculation(input = {}, options = {}) {
    let path = "/reports/financial-runs";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createSocialChannel(input = {}, options = {}) {
    let path = "/social/channels";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createSocialPublication(input = {}, options = {}) {
    let path = "/social/publications";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createSyncPolicy(input = {}, options = {}) {
    let path = "/sync/policies";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createUpload(input = {}, options = {}) {
    let path = "/uploads";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createWarehouseMobilePack(input = {}, options = {}) {
    let path = "/warehouse-mobile/packs";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createWarehouseMobilePickBatch(input = {}, options = {}) {
    let path = "/warehouse-mobile/batches";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createWarehouseMobilePickBatchForPlan(input = {}, options = {}) {
    let path = "/warehouse-mobile/plans/{plan_id}/pick-batches";
    if (typeof input.planId !== "string" || input.planId.trim() === "") throw new TypeError("plan_id is required");
    path = path.replace("{plan_id}", encodeURIComponent(input.planId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createWarehouseMobilePlan(input = {}, options = {}) {
    let path = "/warehouse-mobile/plans";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createWarehouseTask(input = {}, options = {}) {
    let path = "/warehouse-tasks";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createWarehouseTaskBatch(input = {}, options = {}) {
    let path = "/warehouse-task-batches";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createWarehouseTasksFromOrder(input = {}, options = {}) {
    let path = "/warehouse-tasks/from-order";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createWebhookSubscription(input = {}, options = {}) {
    let path = "/webhook-subscriptions";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async createWorkflow(input = {}, options = {}) {
    let path = "/workflows";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async decideApproval(input = {}, options = {}) {
    let path = "/approvals/{request_id}/decision";
    if (typeof input.requestId !== "string" || input.requestId.trim() === "") throw new TypeError("request_id is required");
    path = path.replace("{request_id}", encodeURIComponent(input.requestId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async deleteCurrentUserAvatar(input = {}, options = {}) {
    let path = "/me/profile/avatar";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("DELETE", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async deleteDemoOrders(input = {}, options = {}) {
    let path = "/orders/demo";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("DELETE", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async deleteLogisticsSeparateReturn(input = {}, options = {}) {
    let path = "/logistics/returns/separate/{return_id}";
    if (typeof input.returnId !== "string" || input.returnId.trim() === "") throw new TypeError("return_id is required");
    path = path.replace("{return_id}", encodeURIComponent(input.returnId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("DELETE", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async deleteProductImage(input = {}, options = {}) {
    let path = "/products/{product_id}/images/{image_id}";
    if (typeof input.productId !== "string" || input.productId.trim() === "") throw new TypeError("product_id is required");
    path = path.replace("{product_id}", encodeURIComponent(input.productId));
    if (typeof input.imageId !== "string" || input.imageId.trim() === "") throw new TypeError("image_id is required");
    path = path.replace("{image_id}", encodeURIComponent(input.imageId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("DELETE", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async deleteSocialPublication(input = {}, options = {}) {
    let path = "/social/publications/{publication_id}";
    if (typeof input.publicationId !== "string" || input.publicationId.trim() === "") throw new TypeError("publication_id is required");
    path = path.replace("{publication_id}", encodeURIComponent(input.publicationId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("DELETE", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async disableAIProviderAccount(input = {}, options = {}) {
    let path = "/settings/ai-providers:disable";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async disableConnectorAccount(input = {}, options = {}) {
    let path = "/connector-accounts:disable";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async disableMCPAccount(input = {}, options = {}) {
    let path = "/settings/mcp-accounts:disable";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async disableSettingsIdentityProvider(input = {}, options = {}) {
    let path = "/settings/identity-providers/{provider_id}:disable";
    if (typeof input.providerId !== "string" || input.providerId.trim() === "") throw new TypeError("provider_id is required");
    path = path.replace("{provider_id}", encodeURIComponent(input.providerId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async disableWebhookSubscription(input = {}, options = {}) {
    let path = "/webhook-subscriptions/{subscription_id}";
    if (typeof input.subscriptionId !== "string" || input.subscriptionId.trim() === "") throw new TypeError("subscription_id is required");
    path = path.replace("{subscription_id}", encodeURIComponent(input.subscriptionId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("DELETE", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async editLogisticsSeparateReturn(input = {}, options = {}) {
    let path = "/logistics/returns/separate/{return_id}";
    if (typeof input.returnId !== "string" || input.returnId.trim() === "") throw new TypeError("return_id is required");
    path = path.replace("{return_id}", encodeURIComponent(input.returnId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async editSocialPublication(input = {}, options = {}) {
    let path = "/social/publications/{publication_id}";
    if (typeof input.publicationId !== "string" || input.publicationId.trim() === "") throw new TypeError("publication_id is required");
    path = path.replace("{publication_id}", encodeURIComponent(input.publicationId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async enableConnectorAccount(input = {}, options = {}) {
    let path = "/connector-accounts:enable";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async enqueueMarketplacePublication(input = {}, options = {}) {
    let path = "/marketplace-publications";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async enrollConnectorCredentials(input = {}, options = {}) {
    let path = "/connector-accounts:credentials";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async evaluateEntitlement(input = {}, options = {}) {
    let path = "/entitlements/evaluate";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async evaluateProductCompliance(input = {}, options = {}) {
    let path = "/compliance/evaluate";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async exceptionWarehouseTask(input = {}, options = {}) {
    let path = "/warehouse-tasks/{task_id}/exception";
    if (typeof input.taskId !== "string" || input.taskId.trim() === "") throw new TypeError("task_id is required");
    path = path.replace("{task_id}", encodeURIComponent(input.taskId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async executeApproval(input = {}, options = {}) {
    let path = "/approvals/{request_id}/execute";
    if (typeof input.requestId !== "string" || input.requestId.trim() === "") throw new TypeError("request_id is required");
    path = path.replace("{request_id}", encodeURIComponent(input.requestId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getAIEgressPolicy(options = {}) {
    let path = "/settings/ai-egress-policy";
    return this._request("GET", path, {}, undefined, options);
  }

  async getAssistantRun(input = {}, options = {}) {
    let path = "/assistant/runs/{run_id}";
    if (typeof input.runId !== "string" || input.runId.trim() === "") throw new TypeError("run_id is required");
    path = path.replace("{run_id}", encodeURIComponent(input.runId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getAssistantSession(input = {}, options = {}) {
    let path = "/assistant/sessions/{session_id}";
    if (typeof input.sessionId !== "string" || input.sessionId.trim() === "") throw new TypeError("session_id is required");
    path = path.replace("{session_id}", encodeURIComponent(input.sessionId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getCatalogBulkKillSwitch(options = {}) {
    let path = "/catalog/bulk/kill-switch";
    return this._request("GET", path, {}, undefined, options);
  }

  async getCatalogBulkPreview(input = {}, options = {}) {
    let path = "/catalog/bulk/previews/{preview_id}";
    if (typeof input.previewId !== "string" || input.previewId.trim() === "") throw new TypeError("preview_id is required");
    path = path.replace("{preview_id}", encodeURIComponent(input.previewId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getCatalogBulkRun(input = {}, options = {}) {
    let path = "/catalog/bulk/runs/{run_id}";
    if (typeof input.runId !== "string" || input.runId.trim() === "") throw new TypeError("run_id is required");
    path = path.replace("{run_id}", encodeURIComponent(input.runId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getCloudSubscription(options = {}) {
    let path = "/cloud/subscription";
    return this._request("GET", path, {}, undefined, options);
  }

  async getConnectorBootstrapState(options = {}) {
    let path = "/connector-accounts:bootstrap";
    return this._request("GET", path, {}, undefined, options);
  }

  async getConnectorReadiness(input = {}, options = {}) {
    let path = "/connector-readiness/{connector_id}";
    if (typeof input.connectorId !== "string" || input.connectorId.trim() === "") throw new TypeError("connector_id is required");
    path = path.replace("{connector_id}", encodeURIComponent(input.connectorId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getConnectorRuntimeConfig(input = {}, options = {}) {
    let path = "/connector-accounts:runtime-config";
    const query = {};
    if (typeof input.accountId !== "string" || input.accountId.trim() === "") throw new TypeError("account_id is required");
    if (input.accountId !== undefined) query["account_id"] = input.accountId;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getCurrentUserProfile(options = {}) {
    let path = "/me/profile";
    return this._request("GET", path, {}, undefined, options);
  }

  async getCustomerServiceCustomerTimeline(input = {}, options = {}) {
    let path = "/customer-service/customers/{customer_ref_id}";
    if (typeof input.customerRefId !== "string" || input.customerRefId.trim() === "") throw new TypeError("customer_ref_id is required");
    path = path.replace("{customer_ref_id}", encodeURIComponent(input.customerRefId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getCustomerServiceSummary(options = {}) {
    let path = "/customer-service/summary";
    return this._request("GET", path, {}, undefined, options);
  }

  async getCustomerServiceThread(input = {}, options = {}) {
    let path = "/customer-service/threads/{conversation_id}";
    if (typeof input.conversationId !== "string" || input.conversationId.trim() === "") throw new TypeError("conversation_id is required");
    path = path.replace("{conversation_id}", encodeURIComponent(input.conversationId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getEcosystemMetrics(options = {}) {
    let path = "/ecosystem/metrics";
    return this._request("GET", path, {}, undefined, options);
  }

  async getEcosystemOverview(options = {}) {
    let path = "/ecosystem/overview";
    return this._request("GET", path, {}, undefined, options);
  }

  async getFinancialCompleteness(input = {}, options = {}) {
    let path = "/financial-completeness";
    const query = {};
    if (input.basis !== undefined) query["basis"] = input.basis;
    if (input.from !== undefined) query["from"] = input.from;
    if (input.to !== undefined) query["to"] = input.to;
    if (input.currency !== undefined) query["currency"] = input.currency;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getFulfillmentAllocation(input = {}, options = {}) {
    let path = "/inventory/fulfillment-allocations/{allocation_id}";
    if (typeof input.allocationId !== "string" || input.allocationId.trim() === "") throw new TypeError("allocation_id is required");
    path = path.replace("{allocation_id}", encodeURIComponent(input.allocationId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getHealth(options = {}) {
    let path = "/health";
    return this._request("GET", path, {}, undefined, options);
  }

  async getIntegrationCenter(input = {}, options = {}) {
    let path = "/integration-center";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    if (input.family !== undefined) query["family"] = input.family;
    if (input.surface !== undefined) query["surface"] = input.surface;
    if (input.overall !== undefined) query["overall"] = input.overall;
    if (input.health !== undefined) query["health"] = input.health;
    if (input.sync !== undefined) query["sync"] = input.sync;
    if (input.capability !== undefined) query["capability"] = input.capability;
    if (input.issue !== undefined) query["issue"] = input.issue;
    if (input.stale !== undefined) query["stale"] = input.stale;
    if (input.accountId !== undefined) query["account_id"] = input.accountId;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getIntegrationCenterAccount(input = {}, options = {}) {
    let path = "/integration-center/{account_id}";
    if (typeof input.accountId !== "string" || input.accountId.trim() === "") throw new TypeError("account_id is required");
    path = path.replace("{account_id}", encodeURIComponent(input.accountId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getInventoryPosition(input = {}, options = {}) {
    let path = "/inventory/positions/{position_id}";
    if (typeof input.positionId !== "string" || input.positionId.trim() === "") throw new TypeError("position_id is required");
    path = path.replace("{position_id}", encodeURIComponent(input.positionId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getLineageTimeline(input = {}, options = {}) {
    let path = "/lineage/timeline";
    const query = {};
    if (typeof input.system !== "string" || input.system.trim() === "") throw new TypeError("system is required");
    if (input.system !== undefined) query["system"] = input.system;
    if (typeof input.entityType !== "string" || input.entityType.trim() === "") throw new TypeError("entity_type is required");
    if (input.entityType !== undefined) query["entity_type"] = input.entityType;
    if (typeof input.entityId !== "string" || input.entityId.trim() === "") throw new TypeError("entity_id is required");
    if (input.entityId !== undefined) query["entity_id"] = input.entityId;
    if (input.field !== undefined) query["field"] = input.field;
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.beforeAt !== undefined) query["before_at"] = input.beforeAt;
    if (input.beforeId !== undefined) query["before_id"] = input.beforeId;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getLogisticsBatch(input = {}, options = {}) {
    let path = "/logistics/batches/{batch_id}";
    if (typeof input.batchId !== "string" || input.batchId.trim() === "") throw new TypeError("batch_id is required");
    path = path.replace("{batch_id}", encodeURIComponent(input.batchId));
    const query = {};
    if (typeof input.connectorAccountId !== "string" || input.connectorAccountId.trim() === "") throw new TypeError("connector_account_id is required");
    if (input.connectorAccountId !== undefined) query["connector_account_id"] = input.connectorAccountId;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getLogisticsOrder(input = {}, options = {}) {
    let path = "/logistics/orders/{order_id}";
    if (typeof input.orderId !== "string" || input.orderId.trim() === "") throw new TypeError("order_id is required");
    path = path.replace("{order_id}", encodeURIComponent(input.orderId));
    const query = {};
    if (typeof input.connectorAccountId !== "string" || input.connectorAccountId.trim() === "") throw new TypeError("connector_account_id is required");
    if (input.connectorAccountId !== undefined) query["connector_account_id"] = input.connectorAccountId;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getMCPAccountPolicy(input = {}, options = {}) {
    let path = "/settings/mcp-accounts/{account_id}:policy";
    if (typeof input.accountId !== "string" || input.accountId.trim() === "") throw new TypeError("account_id is required");
    path = path.replace("{account_id}", encodeURIComponent(input.accountId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getMCPAgentKillSwitch(options = {}) {
    let path = "/settings/mcp-agents:kill-switch";
    return this._request("GET", path, {}, undefined, options);
  }

  async getMarketplaceGrowthKillSwitch(options = {}) {
    let path = "/marketplace-growth/kill-switch";
    return this._request("GET", path, {}, undefined, options);
  }

  async getMarketplaceGrowthOperation(input = {}, options = {}) {
    let path = "/marketplace-growth/operations/{operation_id}";
    if (typeof input.operationId !== "string" || input.operationId.trim() === "") throw new TypeError("operation_id is required");
    path = path.replace("{operation_id}", encodeURIComponent(input.operationId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getMarketplaceGrowthPreview(input = {}, options = {}) {
    let path = "/marketplace-growth/previews/{preview_id}";
    if (typeof input.previewId !== "string" || input.previewId.trim() === "") throw new TypeError("preview_id is required");
    path = path.replace("{preview_id}", encodeURIComponent(input.previewId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getMarketplaceListingBatch(input = {}, options = {}) {
    let path = "/marketplace-listings/batches/{batch_id}";
    if (typeof input.batchId !== "string" || input.batchId.trim() === "") throw new TypeError("batch_id is required");
    path = path.replace("{batch_id}", encodeURIComponent(input.batchId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getMarketplaceListingTaxonomy(input = {}, options = {}) {
    let path = "/marketplace-listings/taxonomy";
    const query = {};
    if (input.taxonomyId !== undefined) query["taxonomy_id"] = input.taxonomyId;
    if (input.connectorId !== undefined) query["connector_id"] = input.connectorId;
    if (input.locale !== undefined) query["locale"] = input.locale;
    if (input.jurisdiction !== undefined) query["jurisdiction"] = input.jurisdiction;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getMarketplaceOperationFlow(input = {}, options = {}) {
    let path = "/marketplace-operations/flows/{flow_id}";
    if (typeof input.flowId !== "string" || input.flowId.trim() === "") throw new TypeError("flow_id is required");
    path = path.replace("{flow_id}", encodeURIComponent(input.flowId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getMarketplaceOperations(input = {}, options = {}) {
    let path = "/marketplace-operations/{account_id}";
    if (typeof input.accountId !== "string" || input.accountId.trim() === "") throw new TypeError("account_id is required");
    path = path.replace("{account_id}", encodeURIComponent(input.accountId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getMarketplacePublication(input = {}, options = {}) {
    let path = "/marketplace-publications/{operation_id}";
    if (typeof input.operationId !== "string" || input.operationId.trim() === "") throw new TypeError("operation_id is required");
    path = path.replace("{operation_id}", encodeURIComponent(input.operationId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getMarkingOverview(input = {}, options = {}) {
    let path = "/marking/overview";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getNotificationPreference(input = {}, options = {}) {
    let path = "/notification-preferences/{channel}";
    if (typeof input.channel !== "string" || input.channel.trim() === "") throw new TypeError("channel is required");
    path = path.replace("{channel}", encodeURIComponent(input.channel));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getOrder(input = {}, options = {}) {
    let path = "/orders/{order_id}";
    if (typeof input.orderId !== "string" || input.orderId.trim() === "") throw new TypeError("order_id is required");
    path = path.replace("{order_id}", encodeURIComponent(input.orderId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getOrderCancellation(input = {}, options = {}) {
    let path = "/order-cancellations/{cancellation_id}";
    if (typeof input.cancellationId !== "string" || input.cancellationId.trim() === "") throw new TypeError("cancellation_id is required");
    path = path.replace("{cancellation_id}", encodeURIComponent(input.cancellationId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getPayment(input = {}, options = {}) {
    let path = "/payments/{payment_id}";
    if (typeof input.paymentId !== "string" || input.paymentId.trim() === "") throw new TypeError("payment_id is required");
    path = path.replace("{payment_id}", encodeURIComponent(input.paymentId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getProcurementPurchaseOrder(input = {}, options = {}) {
    let path = "/procurement/purchase-orders/{purchase_order_id}";
    if (typeof input.purchaseOrderId !== "string" || input.purchaseOrderId.trim() === "") throw new TypeError("purchase_order_id is required");
    path = path.replace("{purchase_order_id}", encodeURIComponent(input.purchaseOrderId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getProcurementSupplier(input = {}, options = {}) {
    let path = "/procurement/suppliers/{supplier_id}";
    if (typeof input.supplierId !== "string" || input.supplierId.trim() === "") throw new TypeError("supplier_id is required");
    path = path.replace("{supplier_id}", encodeURIComponent(input.supplierId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getProduct(input = {}, options = {}) {
    let path = "/products/{product_id}";
    if (typeof input.productId !== "string" || input.productId.trim() === "") throw new TypeError("product_id is required");
    path = path.replace("{product_id}", encodeURIComponent(input.productId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getPublicationQualityReceipt(input = {}, options = {}) {
    let path = "/publication-quality/receipts/{receipt_id}";
    if (typeof input.receiptId !== "string" || input.receiptId.trim() === "") throw new TypeError("receipt_id is required");
    path = path.replace("{receipt_id}", encodeURIComponent(input.receiptId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getPublicationQualityRun(input = {}, options = {}) {
    let path = "/publication-quality/runs/{run_id}";
    if (typeof input.runId !== "string" || input.runId.trim() === "") throw new TypeError("run_id is required");
    path = path.replace("{run_id}", encodeURIComponent(input.runId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getReportData(input = {}, options = {}) {
    let path = "/reports/{report_id}";
    if (typeof input.reportId !== "string" || input.reportId.trim() === "") throw new TypeError("report_id is required");
    path = path.replace("{report_id}", encodeURIComponent(input.reportId));
    const query = {};
    if (input.from !== undefined) query["from"] = input.from;
    if (input.to !== undefined) query["to"] = input.to;
    if (input.q !== undefined) query["q"] = input.q;
    if (input.currency !== undefined) query["currency"] = input.currency;
    if (input.status !== undefined) query["status"] = input.status;
    if (input.completeness !== undefined) query["completeness"] = input.completeness;
    if (input.basis !== undefined) query["basis"] = input.basis;
    if (input.channelRef !== undefined) query["channel_ref"] = input.channelRef;
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.format !== undefined) query["format"] = input.format;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getReturn(input = {}, options = {}) {
    let path = "/returns/{return_id}";
    if (typeof input.returnId !== "string" || input.returnId.trim() === "") throw new TypeError("return_id is required");
    path = path.replace("{return_id}", encodeURIComponent(input.returnId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getReturnLogistics(input = {}, options = {}) {
    let path = "/return-logistics/{operation_id}";
    if (typeof input.operationId !== "string" || input.operationId.trim() === "") throw new TypeError("operation_id is required");
    path = path.replace("{operation_id}", encodeURIComponent(input.operationId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getRuntimeSecurityPosture(options = {}) {
    let path = "/settings/security/posture";
    return this._request("GET", path, {}, undefined, options);
  }

  async getSellerCashFlow(input = {}, options = {}) {
    let path = "/reports/seller_cash_flow";
    const query = {};
    if (input.from !== undefined) query["from"] = input.from;
    if (input.to !== undefined) query["to"] = input.to;
    if (input.runId !== undefined) query["run_id"] = input.runId;
    if (input.currency !== undefined) query["currency"] = input.currency;
    if (input.channelRef !== undefined) query["channel_ref"] = input.channelRef;
    if (input.q !== undefined) query["q"] = input.q;
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    if (input.format !== undefined) query["format"] = input.format;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getSellerFinancialQuality(input = {}, options = {}) {
    let path = "/reports/seller_financial_quality";
    const query = {};
    if (input.from !== undefined) query["from"] = input.from;
    if (input.to !== undefined) query["to"] = input.to;
    if (input.runId !== undefined) query["run_id"] = input.runId;
    if (input.currency !== undefined) query["currency"] = input.currency;
    if (input.channelRef !== undefined) query["channel_ref"] = input.channelRef;
    if (input.q !== undefined) query["q"] = input.q;
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    if (input.format !== undefined) query["format"] = input.format;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getSellerProfitAndLoss(input = {}, options = {}) {
    let path = "/reports/seller_profit_and_loss";
    const query = {};
    if (input.from !== undefined) query["from"] = input.from;
    if (input.to !== undefined) query["to"] = input.to;
    if (input.runId !== undefined) query["run_id"] = input.runId;
    if (input.currency !== undefined) query["currency"] = input.currency;
    if (input.channelRef !== undefined) query["channel_ref"] = input.channelRef;
    if (input.q !== undefined) query["q"] = input.q;
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    if (input.format !== undefined) query["format"] = input.format;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getSellerProfitAndLossDetails(input = {}, options = {}) {
    let path = "/reports/seller_profit_and_loss/details";
    const query = {};
    if (input.from !== undefined) query["from"] = input.from;
    if (input.to !== undefined) query["to"] = input.to;
    if (input.runId !== undefined) query["run_id"] = input.runId;
    if (input.currency !== undefined) query["currency"] = input.currency;
    if (input.channelRef !== undefined) query["channel_ref"] = input.channelRef;
    if (input.sku !== undefined) query["sku"] = input.sku;
    if (input.orderId !== undefined) query["order_id"] = input.orderId;
    if (input.q !== undefined) query["q"] = input.q;
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    if (input.format !== undefined) query["format"] = input.format;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getSellerUnitEconomics(input = {}, options = {}) {
    let path = "/reports/seller_unit_economics";
    const query = {};
    if (input.from !== undefined) query["from"] = input.from;
    if (input.to !== undefined) query["to"] = input.to;
    if (input.runId !== undefined) query["run_id"] = input.runId;
    if (input.currency !== undefined) query["currency"] = input.currency;
    if (input.channelRef !== undefined) query["channel_ref"] = input.channelRef;
    if (input.q !== undefined) query["q"] = input.q;
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    if (input.format !== undefined) query["format"] = input.format;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getSettingsSecurityConfiguration(options = {}) {
    let path = "/settings/security/configuration";
    return this._request("GET", path, {}, undefined, options);
  }

  async getSyncStatus(options = {}) {
    let path = "/sync/status";
    return this._request("GET", path, {}, undefined, options);
  }

  async getUpload(input = {}, options = {}) {
    let path = "/uploads/{upload_id}";
    if (typeof input.uploadId !== "string" || input.uploadId.trim() === "") throw new TypeError("upload_id is required");
    path = path.replace("{upload_id}", encodeURIComponent(input.uploadId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getUploadContent(input = {}, options = {}) {
    let path = "/uploads/{upload_id}/content";
    if (typeof input.uploadId !== "string" || input.uploadId.trim() === "") throw new TypeError("upload_id is required");
    path = path.replace("{upload_id}", encodeURIComponent(input.uploadId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getWarehouseIncident(input = {}, options = {}) {
    let path = "/inventory/warehouse-incidents/{incident_id}";
    if (typeof input.incidentId !== "string" || input.incidentId.trim() === "") throw new TypeError("incident_id is required");
    path = path.replace("{incident_id}", encodeURIComponent(input.incidentId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getWarehouseMobilePlan(input = {}, options = {}) {
    let path = "/warehouse-mobile/plans/{plan_id}";
    if (typeof input.planId !== "string" || input.planId.trim() === "") throw new TypeError("plan_id is required");
    path = path.replace("{plan_id}", encodeURIComponent(input.planId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getWarehouseMobileSummary(options = {}) {
    let path = "/warehouse-mobile/summary";
    return this._request("GET", path, {}, undefined, options);
  }

  async getWarehouseOperationalState(input = {}, options = {}) {
    let path = "/inventory/warehouses/{warehouse_id}/operational-state";
    if (typeof input.warehouseId !== "string" || input.warehouseId.trim() === "") throw new TypeError("warehouse_id is required");
    path = path.replace("{warehouse_id}", encodeURIComponent(input.warehouseId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getWarehouseTask(input = {}, options = {}) {
    let path = "/warehouse-tasks/{task_id}";
    if (typeof input.taskId !== "string" || input.taskId.trim() === "") throw new TypeError("task_id is required");
    path = path.replace("{task_id}", encodeURIComponent(input.taskId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getWarehouseTaskBatch(input = {}, options = {}) {
    let path = "/warehouse-task-batches/{batch_id}";
    if (typeof input.batchId !== "string" || input.batchId.trim() === "") throw new TypeError("batch_id is required");
    path = path.replace("{batch_id}", encodeURIComponent(input.batchId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getWebhookDeliveryHistory(input = {}, options = {}) {
    let path = "/webhook-deliveries/{delivery_id}/history";
    if (typeof input.deliveryId !== "string" || input.deliveryId.trim() === "") throw new TypeError("delivery_id is required");
    path = path.replace("{delivery_id}", encodeURIComponent(input.deliveryId));
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getWorkflow(input = {}, options = {}) {
    let path = "/workflows/{workflow_id}";
    if (typeof input.workflowId !== "string" || input.workflowId.trim() === "") throw new TypeError("workflow_id is required");
    path = path.replace("{workflow_id}", encodeURIComponent(input.workflowId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getWorkflowRun(input = {}, options = {}) {
    let path = "/workflow-runs/{run_id}";
    if (typeof input.runId !== "string" || input.runId.trim() === "") throw new TypeError("run_id is required");
    path = path.replace("{run_id}", encodeURIComponent(input.runId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getWorkspaceMemberProfile(input = {}, options = {}) {
    let path = "/settings/members/{member_id}/profile";
    if (typeof input.memberId !== "string" || input.memberId.trim() === "") throw new TypeError("member_id is required");
    path = path.replace("{member_id}", encodeURIComponent(input.memberId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async getWorkspaceSettings(options = {}) {
    let path = "/settings/workspace";
    return this._request("GET", path, {}, undefined, options);
  }

  async handoffWarehouseTaskBatch(input = {}, options = {}) {
    let path = "/warehouse-task-batches/{batch_id}/handoff";
    if (typeof input.batchId !== "string" || input.batchId.trim() === "") throw new TypeError("batch_id is required");
    path = path.replace("{batch_id}", encodeURIComponent(input.batchId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async importInventory(input = {}, options = {}) {
    let path = "/inventory/import";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async ingestCustomerServiceInbound(input = {}, options = {}) {
    let path = "/customer-service/inbound";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async installMCPAccountPolicy(input = {}, options = {}) {
    let path = "/settings/mcp-accounts/{account_id}:install-policy";
    if (typeof input.accountId !== "string" || input.accountId.trim() === "") throw new TypeError("account_id is required");
    path = path.replace("{account_id}", encodeURIComponent(input.accountId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async inviteWorkspaceMember(input = {}, options = {}) {
    let path = "/settings/members";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async listAIProviderAccounts(options = {}) {
    let path = "/settings/ai-providers";
    return this._request("GET", path, {}, undefined, options);
  }

  async listAdvertisingCampaigns(input = {}, options = {}) {
    let path = "/advertising/campaigns";
    const query = {};
    if (input.channel !== undefined) query["channel"] = input.channel;
    if (input.campaignId !== undefined) query["campaign_id"] = input.campaignId;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listAdvertisingMetrics(input = {}, options = {}) {
    let path = "/advertising/metrics";
    const query = {};
    if (input.from !== undefined) query["from"] = input.from;
    if (input.to !== undefined) query["to"] = input.to;
    if (input.channel !== undefined) query["channel"] = input.channel;
    if (input.campaignId !== undefined) query["campaign_id"] = input.campaignId;
    if (input.sku !== undefined) query["sku"] = input.sku;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listAdvertisingPerformance(input = {}, options = {}) {
    let path = "/advertising/performance";
    const query = {};
    if (input.from !== undefined) query["from"] = input.from;
    if (input.to !== undefined) query["to"] = input.to;
    if (input.channel !== undefined) query["channel"] = input.channel;
    if (input.campaignId !== undefined) query["campaign_id"] = input.campaignId;
    if (input.sku !== undefined) query["sku"] = input.sku;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listAdvertisingReconciliationFindings(input = {}, options = {}) {
    let path = "/advertising/reconciliation";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listAdvertisingSpend(input = {}, options = {}) {
    let path = "/advertising/spend";
    const query = {};
    if (input.from !== undefined) query["from"] = input.from;
    if (input.to !== undefined) query["to"] = input.to;
    if (input.channel !== undefined) query["channel"] = input.channel;
    if (input.campaignId !== undefined) query["campaign_id"] = input.campaignId;
    if (input.sku !== undefined) query["sku"] = input.sku;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listAdvertisingSyncRuns(input = {}, options = {}) {
    let path = "/advertising/sync-runs";
    const query = {};
    if (input.accountId !== undefined) query["account_id"] = input.accountId;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listApprovalPolicies(options = {}) {
    let path = "/approval-policies";
    return this._request("GET", path, {}, undefined, options);
  }

  async listApprovals(options = {}) {
    let path = "/approvals";
    return this._request("GET", path, {}, undefined, options);
  }

  async listArchivedLogisticsBatches(input = {}, options = {}) {
    let path = "/logistics/batches/archive";
    const query = {};
    if (typeof input.connectorAccountId !== "string" || input.connectorAccountId.trim() === "") throw new TypeError("connector_account_id is required");
    if (input.connectorAccountId !== undefined) query["connector_account_id"] = input.connectorAccountId;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listAssistantSessions(input = {}, options = {}) {
    let path = "/assistant/sessions";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listAuditRecords(input = {}, options = {}) {
    let path = "/audit";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listCatalogBulkChannels(options = {}) {
    let path = "/catalog/bulk/summary";
    return this._request("GET", path, {}, undefined, options);
  }

  async listCatalogBulkPreviews(input = {}, options = {}) {
    let path = "/catalog/bulk/previews";
    const query = {};
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listCatalogBulkRuns(input = {}, options = {}) {
    let path = "/catalog/bulk/runs";
    const query = {};
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listCatalogCategories(options = {}) {
    let path = "/catalog/categories";
    return this._request("GET", path, {}, undefined, options);
  }

  async listComplianceDocuments(options = {}) {
    let path = "/compliance/documents";
    return this._request("GET", path, {}, undefined, options);
  }

  async listConnectorAccountHealthHistory(input = {}, options = {}) {
    let path = "/connector-accounts:health-history";
    const query = {};
    if (typeof input.accountId !== "string" || input.accountId.trim() === "") throw new TypeError("account_id is required");
    if (input.accountId !== undefined) query["account_id"] = input.accountId;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listConnectorAccounts(input = {}, options = {}) {
    let path = "/connector-accounts";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listConnectorReadiness(input = {}, options = {}) {
    let path = "/connector-readiness";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    if (input.family !== undefined) query["family"] = input.family;
    if (input.surface !== undefined) query["surface"] = input.surface;
    if (input.status !== undefined) query["status"] = input.status;
    if (input.priority !== undefined) query["priority"] = input.priority;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listConnectors(options = {}) {
    let path = "/connectors";
    return this._request("GET", path, {}, undefined, options);
  }

  async listCounterparties(input = {}, options = {}) {
    let path = "/counterparties";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listCustomerServiceFindings(input = {}, options = {}) {
    let path = "/customer-service/findings";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listCustomerServiceInbox(input = {}, options = {}) {
    let path = "/customer-service/inbox";
    const query = {};
    if (input.state !== undefined) query["state"] = input.state;
    if (input.type !== undefined) query["type"] = input.type;
    if (input.priority !== undefined) query["priority"] = input.priority;
    if (input.slaState !== undefined) query["sla_state"] = input.slaState;
    if (input.assigneeId !== undefined) query["assignee_id"] = input.assigneeId;
    if (input.teamId !== undefined) query["team_id"] = input.teamId;
    if (input.customerRefId !== undefined) query["customer_ref_id"] = input.customerRefId;
    if (input.unresolved !== undefined) query["unresolved"] = input.unresolved;
    if (input.search !== undefined) query["search"] = input.search;
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listCustomerServiceQuestions(input = {}, options = {}) {
    let path = "/customer-service/questions";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listCustomerServiceReviews(input = {}, options = {}) {
    let path = "/customer-service/reviews";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listEcosystemOnboarding(options = {}) {
    let path = "/ecosystem/onboarding";
    return this._request("GET", path, {}, undefined, options);
  }

  async listEcosystemPartnerCertifications(options = {}) {
    let path = "/ecosystem/partners/certifications";
    return this._request("GET", path, {}, undefined, options);
  }

  async listFXRates(input = {}, options = {}) {
    let path = "/fx/rates";
    const query = {};
    if (input.base !== undefined) query["base"] = input.base;
    if (input.quote !== undefined) query["quote"] = input.quote;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listFinancialBankAccounts(options = {}) {
    let path = "/financial-completeness/accounts";
    return this._request("GET", path, {}, undefined, options);
  }

  async listFinancialCOGSBackfills(input = {}, options = {}) {
    let path = "/financial-completeness/cogs-backfills";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listFinancialCompletenessFindings(input = {}, options = {}) {
    let path = "/financial-completeness/findings";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listFinancialSources(input = {}, options = {}) {
    let path = "/financial-completeness/sources";
    const query = {};
    if (input.kind !== undefined) query["kind"] = input.kind;
    if (input.quality !== undefined) query["quality"] = input.quality;
    if (input.from !== undefined) query["from"] = input.from;
    if (input.to !== undefined) query["to"] = input.to;
    if (input.basis !== undefined) query["basis"] = input.basis;
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listFulfillmentAllocations(options = {}) {
    let path = "/inventory/fulfillment-allocations";
    return this._request("GET", path, {}, undefined, options);
  }

  async listInventoryPositions(options = {}) {
    let path = "/inventory/positions";
    return this._request("GET", path, {}, undefined, options);
  }

  async listInventoryWarehouses(options = {}) {
    let path = "/inventory/warehouses";
    return this._request("GET", path, {}, undefined, options);
  }

  async listLogisticsBatchOrders(input = {}, options = {}) {
    let path = "/logistics/batches/orders";
    const query = {};
    if (typeof input.connectorAccountId !== "string" || input.connectorAccountId.trim() === "") throw new TypeError("connector_account_id is required");
    if (input.connectorAccountId !== undefined) query["connector_account_id"] = input.connectorAccountId;
    if (typeof input.batchId !== "string" || input.batchId.trim() === "") throw new TypeError("batch_id is required");
    if (input.batchId !== undefined) query["batch_id"] = input.batchId;
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.page !== undefined) query["page"] = input.page;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listLogisticsBatches(input = {}, options = {}) {
    let path = "/logistics/batches";
    const query = {};
    if (typeof input.connectorAccountId !== "string" || input.connectorAccountId.trim() === "") throw new TypeError("connector_account_id is required");
    if (input.connectorAccountId !== undefined) query["connector_account_id"] = input.connectorAccountId;
    if (input.mailType !== undefined) query["mail_type"] = input.mailType;
    if (input.mailCategory !== undefined) query["mail_category"] = input.mailCategory;
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.page !== undefined) query["page"] = input.page;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listLogisticsPickupPoints(input = {}, options = {}) {
    let path = "/logistics/pickup-points";
    const query = {};
    if (typeof input.connectorAccountId !== "string" || input.connectorAccountId.trim() === "") throw new TypeError("connector_account_id is required");
    if (input.connectorAccountId !== undefined) query["connector_account_id"] = input.connectorAccountId;
    if (typeof input.country !== "string" || input.country.trim() === "") throw new TypeError("country is required");
    if (input.country !== undefined) query["country"] = input.country;
    if (typeof input.city !== "string" || input.city.trim() === "") throw new TypeError("city is required");
    if (input.city !== undefined) query["city"] = input.city;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listMCPAccounts(options = {}) {
    let path = "/settings/mcp-accounts";
    return this._request("GET", path, {}, undefined, options);
  }

  async listMarketplaceGrowthDrifts(input = {}, options = {}) {
    let path = "/marketplace-growth/reconciliation";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listMarketplaceGrowthOperations(input = {}, options = {}) {
    let path = "/marketplace-growth/operations";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listMarketplaceGrowthRules(input = {}, options = {}) {
    let path = "/marketplace-growth/rules";
    const query = {};
    if (input.channelId !== undefined) query["channel_id"] = input.channelId;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listMarketplaceOperationFindings(input = {}, options = {}) {
    let path = "/marketplace-operations/findings";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    if (input.flowId !== undefined) query["flow_id"] = input.flowId;
    if (input.status !== undefined) query["status"] = input.status;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listMarketplaceOperationFlowFindings(input = {}, options = {}) {
    let path = "/marketplace-operations/flows/{flow_id}/findings";
    if (typeof input.flowId !== "string" || input.flowId.trim() === "") throw new TypeError("flow_id is required");
    path = path.replace("{flow_id}", encodeURIComponent(input.flowId));
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    if (input.status !== undefined) query["status"] = input.status;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listMarketplaceOperationFlows(input = {}, options = {}) {
    let path = "/marketplace-operations/flows";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listMarketplaceOperations(input = {}, options = {}) {
    let path = "/marketplace-operations";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    if (input.accountId !== undefined) query["account_id"] = input.accountId;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listMarketplacePublicationDrifts(input = {}, options = {}) {
    let path = "/marketplace-publications/{operation_id}/drifts";
    if (typeof input.operationId !== "string" || input.operationId.trim() === "") throw new TypeError("operation_id is required");
    path = path.replace("{operation_id}", encodeURIComponent(input.operationId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listMarketplacePublications(options = {}) {
    let path = "/marketplace-publications";
    return this._request("GET", path, {}, undefined, options);
  }

  async listNotificationDeliveries(input = {}, options = {}) {
    let path = "/notifications/{notification_id}/deliveries";
    if (typeof input.notificationId !== "string" || input.notificationId.trim() === "") throw new TypeError("notification_id is required");
    path = path.replace("{notification_id}", encodeURIComponent(input.notificationId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listNotifications(input = {}, options = {}) {
    let path = "/notifications";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listOrders(input = {}, options = {}) {
    let path = "/orders";
    const query = {};
    if (input.q !== undefined) query["q"] = input.q;
    if (input.status !== undefined) query["status"] = input.status;
    if (input.placedFrom !== undefined) query["placed_from"] = input.placedFrom;
    if (input.placedTo !== undefined) query["placed_to"] = input.placedTo;
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listPayments(input = {}, options = {}) {
    let path = "/payments";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listPlugins(input = {}, options = {}) {
    let path = "/plugins";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listProcurementOffers(input = {}, options = {}) {
    let path = "/procurement/offers";
    const query = {};
    if (input.supplierId !== undefined) query["supplier_id"] = input.supplierId;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listProcurementPurchaseOrders(input = {}, options = {}) {
    let path = "/procurement/purchase-orders";
    const query = {};
    if (input.status !== undefined) query["status"] = input.status;
    if (input.supplierId !== undefined) query["supplier_id"] = input.supplierId;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listProcurementReconciliationFindings(options = {}) {
    let path = "/procurement/reconciliation";
    return this._request("GET", path, {}, undefined, options);
  }

  async listProcurementSuppliers(options = {}) {
    let path = "/procurement/suppliers";
    return this._request("GET", path, {}, undefined, options);
  }

  async listProducts(input = {}, options = {}) {
    let path = "/products";
    const query = {};
    if (input.q !== undefined) query["q"] = input.q;
    if (input.status !== undefined) query["status"] = input.status;
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listPublicationQualityRuns(input = {}, options = {}) {
    let path = "/publication-quality/runs";
    const query = {};
    if (input.productId !== undefined) query["product_id"] = input.productId;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listReplenishmentRuns(input = {}, options = {}) {
    let path = "/replenishment";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listReports(options = {}) {
    let path = "/reports";
    return this._request("GET", path, {}, undefined, options);
  }

  async listReturns(input = {}, options = {}) {
    let path = "/returns";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listSecurityEvidence(input = {}, options = {}) {
    let path = "/settings/security/evidence";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.before !== undefined) query["before"] = input.before;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listSettingsIdentityProviders(options = {}) {
    let path = "/settings/identity-providers";
    return this._request("GET", path, {}, undefined, options);
  }

  async listSettingsSecurityAudit(input = {}, options = {}) {
    let path = "/settings/security/audit";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listSettingsSecurityLogins(input = {}, options = {}) {
    let path = "/settings/security/logins";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listSettingsSecuritySessions(input = {}, options = {}) {
    let path = "/settings/security/sessions";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listSettlementEntries(input = {}, options = {}) {
    let path = "/settlements";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listSocialChannels(input = {}, options = {}) {
    let path = "/social/channels";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listSocialPublications(input = {}, options = {}) {
    let path = "/social/publications";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listWarehouseIncidents(options = {}) {
    let path = "/inventory/warehouse-incidents";
    return this._request("GET", path, {}, undefined, options);
  }

  async listWarehouseMobileDevices(input = {}, options = {}) {
    let path = "/warehouse-mobile/devices";
    const query = {};
    if (input.warehouseId !== undefined) query["warehouse_id"] = input.warehouseId;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listWarehouseMobileOfflineIntents(input = {}, options = {}) {
    let path = "/warehouse-mobile/offline-intents";
    const query = {};
    if (input.deviceId !== undefined) query["device_id"] = input.deviceId;
    if (input.state !== undefined) query["state"] = input.state;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listWarehouseMobilePacks(input = {}, options = {}) {
    let path = "/warehouse-mobile/packs";
    const query = {};
    if (input.planId !== undefined) query["plan_id"] = input.planId;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listWarehouseMobilePickBatches(input = {}, options = {}) {
    let path = "/warehouse-mobile/batches";
    const query = {};
    if (input.planId !== undefined) query["plan_id"] = input.planId;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listWarehouseMobilePlans(input = {}, options = {}) {
    let path = "/warehouse-mobile/plans";
    const query = {};
    if (input.mode !== undefined) query["mode"] = input.mode;
    if (input.state !== undefined) query["state"] = input.state;
    if (input.warehouseId !== undefined) query["warehouse_id"] = input.warehouseId;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listWarehouseMobilePrintJobs(input = {}, options = {}) {
    let path = "/warehouse-mobile/print-jobs";
    const query = {};
    if (input.planId !== undefined) query["plan_id"] = input.planId;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listWarehouseTaskHistory(input = {}, options = {}) {
    let path = "/warehouse-tasks/{task_id}/history";
    if (typeof input.taskId !== "string" || input.taskId.trim() === "") throw new TypeError("task_id is required");
    path = path.replace("{task_id}", encodeURIComponent(input.taskId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listWarehouseTasks(input = {}, options = {}) {
    let path = "/warehouse-tasks";
    const query = {};
    if (input.state !== undefined) query["state"] = input.state;
    if (input.taskType !== undefined) query["task_type"] = input.taskType;
    if (input.warehouseId !== undefined) query["warehouse_id"] = input.warehouseId;
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listWebhookSubscriptions(options = {}) {
    let path = "/webhook-subscriptions";
    return this._request("GET", path, {}, undefined, options);
  }

  async listWorkflowRunEvidence(input = {}, options = {}) {
    let path = "/workflow-runs/{run_id}/evidence";
    if (typeof input.runId !== "string" || input.runId.trim() === "") throw new TypeError("run_id is required");
    path = path.replace("{run_id}", encodeURIComponent(input.runId));
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listWorkflowRunSteps(input = {}, options = {}) {
    let path = "/workflow-runs/{run_id}/steps";
    if (typeof input.runId !== "string" || input.runId.trim() === "") throw new TypeError("run_id is required");
    path = path.replace("{run_id}", encodeURIComponent(input.runId));
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listWorkflowRuns(input = {}, options = {}) {
    let path = "/workflow-runs";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listWorkflows(input = {}, options = {}) {
    let path = "/workflows";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async listWorkspaceMembers(input = {}, options = {}) {
    let path = "/settings/members";
    const query = {};
    if (input.limit !== undefined) query["limit"] = input.limit;
    if (input.cursor !== undefined) query["cursor"] = input.cursor;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async markNotificationRead(input = {}, options = {}) {
    let path = "/notifications/{notification_id}/read";
    if (typeof input.notificationId !== "string" || input.notificationId.trim() === "") throw new TypeError("notification_id is required");
    path = path.replace("{notification_id}", encodeURIComponent(input.notificationId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async markProcurementPurchaseOrderSendUnknown(input = {}, options = {}) {
    let path = "/procurement/purchase-orders/{purchase_order_id}/send-timeout";
    if (typeof input.purchaseOrderId !== "string" || input.purchaseOrderId.trim() === "") throw new TypeError("purchase_order_id is required");
    path = path.replace("{purchase_order_id}", encodeURIComponent(input.purchaseOrderId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async pauseWorkflow(input = {}, options = {}) {
    let path = "/workflow-commands/pause";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async preflightMarketplacePublication(input = {}, options = {}) {
    let path = "/marketplace-publications/preflight";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async previewAIEgress(input = {}, options = {}) {
    let path = "/settings/ai-egress-policy:preview";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async previewCatalogBulk(input = {}, options = {}) {
    let path = "/catalog/bulk/previews";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async previewConnectorBootstrap(input = {}, options = {}) {
    let path = "/connector-accounts:bootstrap-preview";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async previewFinancialBankStatement(input = {}, options = {}) {
    let path = "/financial-completeness/statements:preview";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async previewFinancialCOGSBackfill(input = {}, options = {}) {
    let path = "/financial-completeness/cogs-backfills:preview";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async previewMarketplaceListingBatch(input = {}, options = {}) {
    let path = "/marketplace-listings/batch/preview";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async previewProcurementPriceList(input = {}, options = {}) {
    let path = "/procurement/price-lists/preview";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async previewRepricing(input = {}, options = {}) {
    let path = "/pricing/repricing/preview";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async publishWorkflow(input = {}, options = {}) {
    let path = "/workflow-commands/publish";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async putAIEgressPolicy(input = {}, options = {}) {
    let path = "/settings/ai-egress-policy";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PUT", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async putConnectorRuntimeConfig(input = {}, options = {}) {
    let path = "/connector-accounts:runtime-config";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PUT", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async putConnectorSyncSchedule(input = {}, options = {}) {
    let path = "/connector-accounts:schedule";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PUT", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async putNotificationPreference(input = {}, options = {}) {
    let path = "/notification-preferences/{channel}";
    if (typeof input.channel !== "string" || input.channel.trim() === "") throw new TypeError("channel is required");
    path = path.replace("{channel}", encodeURIComponent(input.channel));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("PUT", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async putWarehouseFailoverRoute(input = {}, options = {}) {
    let path = "/inventory/warehouses/{warehouse_id}/failover-routes/{destination_warehouse_id}";
    if (typeof input.warehouseId !== "string" || input.warehouseId.trim() === "") throw new TypeError("warehouse_id is required");
    path = path.replace("{warehouse_id}", encodeURIComponent(input.warehouseId));
    if (typeof input.destinationWarehouseId !== "string" || input.destinationWarehouseId.trim() === "") throw new TypeError("destination_warehouse_id is required");
    path = path.replace("{destination_warehouse_id}", encodeURIComponent(input.destinationWarehouseId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("PUT", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async queueCustomerServiceReply(input = {}, options = {}) {
    let path = "/customer-service/replies";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async queueWarehouseMobilePrint(input = {}, options = {}) {
    let path = "/warehouse-mobile/print-jobs";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async readAfterWriteMarketplaceListing(input = {}, options = {}) {
    let path = "/marketplace-listings/read-after-write";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async readLogisticsLabel(input = {}, options = {}) {
    let path = "/logistics/labels";
    const query = {};
    if (typeof input.connectorAccountId !== "string" || input.connectorAccountId.trim() === "") throw new TypeError("connector_account_id is required");
    if (input.connectorAccountId !== undefined) query["connector_account_id"] = input.connectorAccountId;
    if (typeof input.remoteId !== "string" || input.remoteId.trim() === "") throw new TypeError("remote_id is required");
    if (input.remoteId !== undefined) query["remote_id"] = input.remoteId;
    if (input.format !== undefined) query["format"] = input.format;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async readLogisticsTracking(input = {}, options = {}) {
    let path = "/logistics/tracking";
    const query = {};
    if (typeof input.connectorAccountId !== "string" || input.connectorAccountId.trim() === "") throw new TypeError("connector_account_id is required");
    if (input.connectorAccountId !== undefined) query["connector_account_id"] = input.connectorAccountId;
    if (typeof input.remoteId !== "string" || input.remoteId.trim() === "") throw new TypeError("remote_id is required");
    if (input.remoteId !== undefined) query["remote_id"] = input.remoteId;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async receiveCommerceWebhook(input = {}, options = {}) {
    let path = "/webhooks/commerce/{connector_id}/{organization_id}/{workspace_id}/{account_id}";
    if (typeof input.connectorId !== "string" || input.connectorId.trim() === "") throw new TypeError("connector_id is required");
    path = path.replace("{connector_id}", encodeURIComponent(input.connectorId));
    if (typeof input.organizationId !== "string" || input.organizationId.trim() === "") throw new TypeError("organization_id is required");
    path = path.replace("{organization_id}", encodeURIComponent(input.organizationId));
    if (typeof input.workspaceId !== "string" || input.workspaceId.trim() === "") throw new TypeError("workspace_id is required");
    path = path.replace("{workspace_id}", encodeURIComponent(input.workspaceId));
    if (typeof input.accountId !== "string" || input.accountId.trim() === "") throw new TypeError("account_id is required");
    path = path.replace("{account_id}", encodeURIComponent(input.accountId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (input.saleorSignature !== undefined) requestHeaders["Saleor-Signature"] = input.saleorSignature;
    if (input.saleorEvent !== undefined) requestHeaders["Saleor-Event"] = input.saleorEvent;
    if (input.xWCWebhookSignature !== undefined) requestHeaders["X-WC-Webhook-Signature"] = input.xWCWebhookSignature;
    if (input.xWCWebhookTopic !== undefined) requestHeaders["X-WC-Webhook-Topic"] = input.xWCWebhookTopic;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async receiveProcurementPurchaseOrder(input = {}, options = {}) {
    let path = "/procurement/purchase-orders/{purchase_order_id}/receive";
    if (typeof input.purchaseOrderId !== "string" || input.purchaseOrderId.trim() === "") throw new TypeError("purchase_order_id is required");
    path = path.replace("{purchase_order_id}", encodeURIComponent(input.purchaseOrderId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async receiveSocialWebhook(input = {}, options = {}) {
    let path = "/webhooks/social/{connector_id}/{organization_id}/{workspace_id}/{account_id}";
    if (typeof input.connectorId !== "string" || input.connectorId.trim() === "") throw new TypeError("connector_id is required");
    path = path.replace("{connector_id}", encodeURIComponent(input.connectorId));
    if (typeof input.organizationId !== "string" || input.organizationId.trim() === "") throw new TypeError("organization_id is required");
    path = path.replace("{organization_id}", encodeURIComponent(input.organizationId));
    if (typeof input.workspaceId !== "string" || input.workspaceId.trim() === "") throw new TypeError("workspace_id is required");
    path = path.replace("{workspace_id}", encodeURIComponent(input.workspaceId));
    if (typeof input.accountId !== "string" || input.accountId.trim() === "") throw new TypeError("account_id is required");
    path = path.replace("{account_id}", encodeURIComponent(input.accountId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (input.xTelegramBotApiSecretToken !== undefined) requestHeaders["X-Telegram-Bot-Api-Secret-Token"] = input.xTelegramBotApiSecretToken;
    if (input.xMaxBotApiSecret !== undefined) requestHeaders["X-Max-Bot-Api-Secret"] = input.xMaxBotApiSecret;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async reconcileCatalogBulkRun(input = {}, options = {}) {
    let path = "/catalog/bulk/runs/{run_id}/reconcile";
    if (typeof input.runId !== "string" || input.runId.trim() === "") throw new TypeError("run_id is required");
    path = path.replace("{run_id}", encodeURIComponent(input.runId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async reconcileMarketplaceGrowthOperation(input = {}, options = {}) {
    let path = "/marketplace-growth/reconciliation";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async recordAssistantFeedback(input = {}, options = {}) {
    let path = "/assistant/feedback";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async recordMarkingScan(input = {}, options = {}) {
    let path = "/marking/scans";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async recordReturnInspection(input = {}, options = {}) {
    let path = "/returns/{return_id}/inspection";
    if (typeof input.returnId !== "string" || input.returnId.trim() === "") throw new TypeError("return_id is required");
    path = path.replace("{return_id}", encodeURIComponent(input.returnId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async recordWarehouseMobilePrintStatus(input = {}, options = {}) {
    let path = "/warehouse-mobile/print-jobs/{print_job_id}/status";
    if (typeof input.printJobId !== "string" || input.printJobId.trim() === "") throw new TypeError("print_job_id is required");
    path = path.replace("{print_job_id}", encodeURIComponent(input.printJobId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async recordWarehouseMobileRemoteObservation(input = {}, options = {}) {
    let path = "/warehouse-mobile/observations";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async recordWarehouseMobileScan(input = {}, options = {}) {
    let path = "/warehouse-mobile/scans";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async refundPayment(input = {}, options = {}) {
    let path = "/payments/{payment_id}/refund";
    if (typeof input.paymentId !== "string" || input.paymentId.trim() === "") throw new TypeError("payment_id is required");
    path = path.replace("{payment_id}", encodeURIComponent(input.paymentId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async registerWarehouseMobileDevice(input = {}, options = {}) {
    let path = "/warehouse-mobile/devices";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async replaceConnectorAccountCapabilities(input = {}, options = {}) {
    let path = "/connector-accounts:capabilities";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PUT", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async replayWarehouseMobileOfflineScan(input = {}, options = {}) {
    let path = "/warehouse-mobile/offline-intents";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async replayWebhookDelivery(input = {}, options = {}) {
    let path = "/webhook-deliveries/{delivery_id}/replay";
    if (typeof input.deliveryId !== "string" || input.deliveryId.trim() === "") throw new TypeError("delivery_id is required");
    path = path.replace("{delivery_id}", encodeURIComponent(input.deliveryId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async reserveFulfillmentAllocation(input = {}, options = {}) {
    let path = "/inventory/fulfillment-allocations";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async resolveSyncDrift(input = {}, options = {}) {
    let path = "/sync/drifts/{drift_id}/actions";
    if (typeof input.driftId !== "string" || input.driftId.trim() === "") throw new TypeError("drift_id is required");
    path = path.replace("{drift_id}", encodeURIComponent(input.driftId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async restoreLogisticsOrders(input = {}, options = {}) {
    let path = "/logistics/orders/restore";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async retireApprovalPolicy(input = {}, options = {}) {
    let path = "/approval-policies/{policy_id}";
    if (typeof input.policyId !== "string" || input.policyId.trim() === "") throw new TypeError("policy_id is required");
    path = path.replace("{policy_id}", encodeURIComponent(input.policyId));
    const query = {};
    if (input.expectedVersion !== undefined) query["expected_version"] = input.expectedVersion;
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("DELETE", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async retryMarketplacePublication(input = {}, options = {}) {
    let path = "/marketplace-publications/{operation_id}/retry";
    if (typeof input.operationId !== "string" || input.operationId.trim() === "") throw new TypeError("operation_id is required");
    path = path.replace("{operation_id}", encodeURIComponent(input.operationId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async retryProcurementPurchaseOrderSend(input = {}, options = {}) {
    let path = "/procurement/purchase-orders/{purchase_order_id}/retry";
    if (typeof input.purchaseOrderId !== "string" || input.purchaseOrderId.trim() === "") throw new TypeError("purchase_order_id is required");
    path = path.replace("{purchase_order_id}", encodeURIComponent(input.purchaseOrderId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async retryWorkflowRun(input = {}, options = {}) {
    let path = "/workflow-run-commands/retry";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async revokeSettingsSecuritySession(input = {}, options = {}) {
    let path = "/settings/security/sessions/{session_ref}:revoke";
    if (typeof input.sessionRef !== "string" || input.sessionRef.trim() === "") throw new TypeError("session_ref is required");
    path = path.replace("{session_ref}", encodeURIComponent(input.sessionRef));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async revokeWarehouseMobileDevice(input = {}, options = {}) {
    let path = "/warehouse-mobile/devices/{device_id}/revoke";
    if (typeof input.deviceId !== "string" || input.deviceId.trim() === "") throw new TypeError("device_id is required");
    path = path.replace("{device_id}", encodeURIComponent(input.deviceId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async rollbackSettingsIdentityProvider(input = {}, options = {}) {
    let path = "/settings/identity-providers/{provider_id}:rollback";
    if (typeof input.providerId !== "string" || input.providerId.trim() === "") throw new TypeError("provider_id is required");
    path = path.replace("{provider_id}", encodeURIComponent(input.providerId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async rotateMCPAccount(input = {}, options = {}) {
    let path = "/settings/mcp-accounts:rotate";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async rotateWebhookSigningSecret(input = {}, options = {}) {
    let path = "/webhook-subscriptions/{subscription_id}/rotate-secret";
    if (typeof input.subscriptionId !== "string" || input.subscriptionId.trim() === "") throw new TypeError("subscription_id is required");
    path = path.replace("{subscription_id}", encodeURIComponent(input.subscriptionId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async runConnectorReplay(input = {}, options = {}) {
    let path = "/settings/connector-replay:run";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async runSyncPolicy(input = {}, options = {}) {
    let path = "/sync/policies/{policy_id}/run";
    if (typeof input.policyId !== "string" || input.policyId.trim() === "") throw new TypeError("policy_id is required");
    path = path.replace("{policy_id}", encodeURIComponent(input.policyId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async runWorkflow(input = {}, options = {}) {
    let path = "/workflow-commands/run";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async saveSettingsIdentityProviderDraft(input = {}, options = {}) {
    let path = "/settings/identity-providers/{provider_id}";
    if (typeof input.providerId !== "string" || input.providerId.trim() === "") throw new TypeError("provider_id is required");
    path = path.replace("{provider_id}", encodeURIComponent(input.providerId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PUT", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async scanWarehouseTask(input = {}, options = {}) {
    let path = "/warehouse-tasks/{task_id}/scan";
    if (typeof input.taskId !== "string" || input.taskId.trim() === "") throw new TypeError("task_id is required");
    path = path.replace("{task_id}", encodeURIComponent(input.taskId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async searchLegalParties(input = {}, options = {}) {
    let path = "/counterparties/search";
    const query = {};
    if (input.q !== undefined) query["q"] = input.q;
    if (input.inn !== undefined) query["inn"] = input.inn;
    if (input.registrationId !== undefined) query["registration_id"] = input.registrationId;
    if (input.partyType !== undefined) query["party_type"] = input.partyType;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async searchLogisticsOrders(input = {}, options = {}) {
    let path = "/logistics/orders/search";
    const query = {};
    if (typeof input.connectorAccountId !== "string" || input.connectorAccountId.trim() === "") throw new TypeError("connector_account_id is required");
    if (input.connectorAccountId !== undefined) query["connector_account_id"] = input.connectorAccountId;
    if (typeof input.externalId !== "string" || input.externalId.trim() === "") throw new TypeError("external_id is required");
    if (input.externalId !== undefined) query["external_id"] = input.externalId;
    if (input.limit !== undefined) query["limit"] = input.limit;
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("GET", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async sendAssistantMessage(input = {}, options = {}) {
    let path = "/assistant/sessions/{session_id}/messages";
    if (typeof input.sessionId !== "string" || input.sessionId.trim() === "") throw new TypeError("session_id is required");
    path = path.replace("{session_id}", encodeURIComponent(input.sessionId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async sendProcurementPurchaseOrder(input = {}, options = {}) {
    let path = "/procurement/purchase-orders/{purchase_order_id}/send";
    if (typeof input.purchaseOrderId !== "string" || input.purchaseOrderId.trim() === "") throw new TypeError("purchase_order_id is required");
    path = path.replace("{purchase_order_id}", encodeURIComponent(input.purchaseOrderId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, undefined, { ...options, headers: requestHeaders });
  }

  async setCatalogBulkKillSwitch(input = {}, options = {}) {
    let path = "/catalog/bulk/kill-switch";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async setMCPAgentKillSwitch(input = {}, options = {}) {
    let path = "/settings/mcp-agents:kill-switch";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async setMarketplaceGrowthKillSwitch(input = {}, options = {}) {
    let path = "/marketplace-growth/kill-switch";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async startConnectorBootstrap(input = {}, options = {}) {
    let path = "/connector-accounts:bootstrap";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async startConnectorOAuth(input = {}, options = {}) {
    let path = "/connector-accounts:oauth-start";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async startWarehouseTask(input = {}, options = {}) {
    let path = "/warehouse-tasks/{task_id}/start";
    if (typeof input.taskId !== "string" || input.taskId.trim() === "") throw new TypeError("task_id is required");
    path = path.replace("{task_id}", encodeURIComponent(input.taskId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async streamRealtimeInvalidations(options = {}) {
    let path = "/realtime";
    return this._request("GET", path, {}, undefined, options);
  }

  async submitLogisticsBatch(input = {}, options = {}) {
    let path = "/logistics/batches/{batch_id}/submit";
    if (typeof input.batchId !== "string" || input.batchId.trim() === "") throw new TypeError("batch_id is required");
    path = path.replace("{batch_id}", encodeURIComponent(input.batchId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async subscribeSocialWebhook(input = {}, options = {}) {
    let path = "/social/webhooks/subscription";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PUT", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async syncConnectorAccount(input = {}, options = {}) {
    let path = "/connector-accounts:sync";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async transitionCustomerServiceConversation(input = {}, options = {}) {
    let path = "/customer-service/transitions";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async unarchiveLogisticsBatch(input = {}, options = {}) {
    let path = "/logistics/batches/archive/revert/{batch_id}";
    if (typeof input.batchId !== "string" || input.batchId.trim() === "") throw new TypeError("batch_id is required");
    path = path.replace("{batch_id}", encodeURIComponent(input.batchId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async unsubscribeSocialWebhook(input = {}, options = {}) {
    let path = "/social/webhooks/subscription";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("DELETE", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async updateCurrentUserProfile(input = {}, options = {}) {
    let path = "/me/profile";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async updateInventoryWarehouse(input = {}, options = {}) {
    let path = "/inventory/warehouses/{warehouse_id}";
    if (typeof input.warehouseId !== "string" || input.warehouseId.trim() === "") throw new TypeError("warehouse_id is required");
    path = path.replace("{warehouse_id}", encodeURIComponent(input.warehouseId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async updateLogisticsBatchSendingDate(input = {}, options = {}) {
    let path = "/logistics/batches/sending-date/{batch_id}";
    if (typeof input.batchId !== "string" || input.batchId.trim() === "") throw new TypeError("batch_id is required");
    path = path.replace("{batch_id}", encodeURIComponent(input.batchId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    if (typeof input.approvalRequestID !== "string" || input.approvalRequestID.trim() === "") throw new TypeError("Approval-Request-ID is required");
    if (input.approvalRequestID !== undefined) requestHeaders["Approval-Request-ID"] = input.approvalRequestID;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async updateOfferPrice(input = {}, options = {}) {
    let path = "/products/{product_id}/prices/{offer_or_price_id}";
    if (typeof input.productId !== "string" || input.productId.trim() === "") throw new TypeError("product_id is required");
    path = path.replace("{product_id}", encodeURIComponent(input.productId));
    if (typeof input.offerOrPriceId !== "string" || input.offerOrPriceId.trim() === "") throw new TypeError("offer_or_price_id is required");
    path = path.replace("{offer_or_price_id}", encodeURIComponent(input.offerOrPriceId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async updateProcurementSupplier(input = {}, options = {}) {
    let path = "/procurement/suppliers/{supplier_id}";
    if (typeof input.supplierId !== "string" || input.supplierId.trim() === "") throw new TypeError("supplier_id is required");
    path = path.replace("{supplier_id}", encodeURIComponent(input.supplierId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async updateProduct(input = {}, options = {}) {
    let path = "/products/{product_id}";
    if (typeof input.productId !== "string" || input.productId.trim() === "") throw new TypeError("product_id is required");
    path = path.replace("{product_id}", encodeURIComponent(input.productId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async updateProductImage(input = {}, options = {}) {
    let path = "/products/{product_id}/images/{image_id}";
    if (typeof input.productId !== "string" || input.productId.trim() === "") throw new TypeError("product_id is required");
    path = path.replace("{product_id}", encodeURIComponent(input.productId));
    if (typeof input.imageId !== "string" || input.imageId.trim() === "") throw new TypeError("image_id is required");
    path = path.replace("{image_id}", encodeURIComponent(input.imageId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async updateProductOffer(input = {}, options = {}) {
    let path = "/products/{product_id}/offers/{offer_id}";
    if (typeof input.productId !== "string" || input.productId.trim() === "") throw new TypeError("product_id is required");
    path = path.replace("{product_id}", encodeURIComponent(input.productId));
    if (typeof input.offerId !== "string" || input.offerId.trim() === "") throw new TypeError("offer_id is required");
    path = path.replace("{offer_id}", encodeURIComponent(input.offerId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async updateSocialChannel(input = {}, options = {}) {
    let path = "/social/channels/{channel_id}";
    if (typeof input.channelId !== "string" || input.channelId.trim() === "") throw new TypeError("channel_id is required");
    path = path.replace("{channel_id}", encodeURIComponent(input.channelId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PUT", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async updateSyncPolicy(input = {}, options = {}) {
    let path = "/sync/policies/{policy_id}";
    if (typeof input.policyId !== "string" || input.policyId.trim() === "") throw new TypeError("policy_id is required");
    path = path.replace("{policy_id}", encodeURIComponent(input.policyId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async updateWarehouseOperationalState(input = {}, options = {}) {
    let path = "/inventory/warehouses/{warehouse_id}/operational-state";
    if (typeof input.warehouseId !== "string" || input.warehouseId.trim() === "") throw new TypeError("warehouse_id is required");
    path = path.replace("{warehouse_id}", encodeURIComponent(input.warehouseId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async updateWorkspaceMember(input = {}, options = {}) {
    let path = "/settings/members/{member_id}";
    if (typeof input.memberId !== "string" || input.memberId.trim() === "") throw new TypeError("member_id is required");
    path = path.replace("{member_id}", encodeURIComponent(input.memberId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async updateWorkspaceMemberProfile(input = {}, options = {}) {
    let path = "/settings/members/{member_id}/profile";
    if (typeof input.memberId !== "string" || input.memberId.trim() === "") throw new TypeError("member_id is required");
    path = path.replace("{member_id}", encodeURIComponent(input.memberId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PATCH", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async updateWorkspaceSettings(input = {}, options = {}) {
    let path = "/settings/workspace";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("PUT", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async uploadCurrentUserAvatar(input = {}, options = {}) {
    let path = "/me/profile/avatar";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async validateSettingsIdentityProvider(input = {}, options = {}) {
    let path = "/settings/identity-providers/{provider_id}:validate";
    if (typeof input.providerId !== "string" || input.providerId.trim() === "") throw new TypeError("provider_id is required");
    path = path.replace("{provider_id}", encodeURIComponent(input.providerId));
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    if (typeof input.idempotencyKey !== "string" || input.idempotencyKey.trim() === "") throw new TypeError("Idempotency-Key is required");
    if (input.idempotencyKey !== undefined) requestHeaders["Idempotency-Key"] = input.idempotencyKey;
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }

  async validateWorkflow(input = {}, options = {}) {
    let path = "/workflow-commands/validate";
    const query = {};
    const requestHeaders = { ...(options.headers || {}) };
    return this._request("POST", path, query, input.body, { ...options, headers: requestHeaders });
  }
}
